name: Test
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Install protobuf compiler (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends -o Acquire::Retries=3 protobuf-compiler
      - name: Install protobuf compiler (Windows)
        if: runner.os == 'Windows'
        run: choco install protoc -y --no-progress
      - name: Install protobuf compiler (macOS)
        if: runner.os == 'macOS'
        run: brew install protobuf
      - name: Install Rust stable
        run: rustup update --no-self-update stable
      - name: Run clippy
        run: cargo clippy --all-targets --all-features
      - name: Run tests
        run: cargo test --workspace -- --nocapture
      - name: Test Web3 without features
        run: cargo test -p anda_web3_client --no-default-features --locked
      - name: Test Web3 client
        run: cargo test -p anda_web3_client --no-default-features --features client --locked
      - name: Test Web3 TEE
        run: cargo test -p anda_web3_client --no-default-features --features tee --locked
      - name: Test full Web3 integration
        run: cargo test -p anda_web3_client --features full --locked
